import { Request, Response } from 'express';
import { subscribeOrderQueue } from '../../services/orderQueueEvents';
export const streamOrderQueue = (req: Request, res: Response) => { const tenantId = req.tenantId!; res.setHeader('Content-Type','text/event-stream'); res.setHeader('Cache-Control','no-cache'); res.setHeader('Connection','keep-alive'); res.flushHeaders?.(); let unsubscribe: (()=>void)|undefined; const heartbeat = setInterval(()=>{ if(!res.writableEnded) res.write(`event: ping\ndata: ${Date.now()}\n\n`); },15000); const cleanup = () => { clearInterval(heartbeat); unsubscribe?.(); }; req.on('close', cleanup); try { unsubscribe = subscribeOrderQueue(tenantId,res); } catch(err) { const msg = err instanceof Error ? err.message : String(err); if(!res.writableEnded){ res.write(`event: error\ndata: ${JSON.stringify({message:msg})}\n\n`); res.end(); } cleanup(); } };
